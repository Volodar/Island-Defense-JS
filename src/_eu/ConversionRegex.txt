1. For variables starting with _
    (.+) _(\w+);
    /** @type {$1} */ _$2 : ,


2. For auto, unsigned
(auto |int |float |unsigned |std.string )
var


3. Class NS to this,  addChild to this
MenuCreateTower::
this.
 addChild
 this.addChild
 removeChild
 this.removeChild
 push_back
 push
assert
EU.assert
.empty() == false
.length > 0
.back()
.slice(-1)[0]


4. void this.functionName to functionName : function, replace create with new
\w+ this.(\w+)
$1: function
(\w+).create
new cc.$1

5. replace property with this.property
propertyName
this.propertyName

6. replace -> with .
->
.

7. replace <type>, const to empty string
<(\w+)*>

const


8. replace :: to .
::
.



7. replace xxf to xx
(\d)f
$1

10. adding ending comma
^    }
    },

11. others
.size()
.length
